{
  "name": "aggle-browser",
  "version": "0.1.0",
  "description": "Aggle - Premium privacy browser layer for LibreWolf",
  "scripts": {
    "dev": "node esbuild.ext.config.mjs",
    "build": "node esbuild.ext.config.mjs prod",
    "start": "web-ext run --source-dir extension --target firefox-desktop"
  },
  "devDependencies": {
    "esbuild": "^0.21.0",
    "typescript": "^5.4.0",
    "web-ext": "^7.8.0"
  }
}
